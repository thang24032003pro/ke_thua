Prompt 1: Tại sao trong Java, khi tôi gọi một hàm được Overload thông qua một biến có kiểu tham chiếu là class Cha (nhưng mang đối tượng class Con), trình biên dịch lại luôn ưu tiên gọi hàm của class Cha? Static Binding là gì?

Prompt 2: Khi tôi chuyển hàm calculateFee() vào bên trong class Order và để trống ruột, tôi nên dùng từ khóa 'abstract' hay cung cấp một logic mặc định? Ưu nhược điểm của 2 cách này đối với khả năng mở rộng là gì?

Prompt 3: Annotation @Override trong Java có bắt buộc không? Nếu tôi gõ sai tên hàm thành calculateFeee() ở lớp con mà không có chữ @Override, trình biên dịch sẽ hiểu lầm đó là chiến lược Overloading hay một phương thức hoàn toàn mới?